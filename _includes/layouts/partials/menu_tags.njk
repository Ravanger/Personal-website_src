<ul id="tagsmenu">
    {% for tag in collections.tagList %}
        {% set tagUrl %}/Projects/{{ tag }}/{% endset %}
        <li class="inline-block"><a href="{{ tagUrl | url }}" {% if tagUrl == page.url %} class="underline" {% endif %}>{{ tag }}</a></li>
    {% endfor %}
</ul>
